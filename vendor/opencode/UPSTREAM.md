# Upstream Source

This directory vendors the current OpenCode source used by XHigh.

- Upstream project: `anomalyco/opencode`
- Upstream package version: `1.2.15`
- License: `MIT`

The vendored workspace is used so `xhigh` can build the OpenCode TUI locally without depending on `opencode.ai/install`.
